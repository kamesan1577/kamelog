# kamelog 開発入口

チャット、Issue、外部サービスの履歴を仕様として必要としない。
最初に以下を順に読む。

1. `spec/invariants/README.md`
2. `spec/current/README.md` と変更対象の仕様
3. `HANDOFF.md`（確認済みと未確認を区別する）
4. 関連する `adr/` と `SECURITY.md`
5. コードと対応するテスト

## 変更手順

- UIを変更する場合は `.codex/skills/kamelog-ui-contract/SKILL.md` を読む。
- 認証・保存・媒体・復元を変更する場合は `.codex/skills/kamelog-data-safety/SKILL.md` を読む。
- 不具合は再現テストを追加してから修正する。失敗をskipやretryで隠さない。
- 設計変更はADR、利用者の振る舞いの変更は現行仕様、実装状況はHANDOFFへ同じ変更で反映する。
- `make check` をローカルで実行し、CIの `check` / `e2e` / `container` をすべて確認する。
- UIのクラス名・CSS・構造をAPI実装の都合で変えない。スナップショットの更新を修正扱いしない。
- 非公開データ・認証情報・環境設定をコミットしない。コミット前に `make public-repo-check` を実行する。
- コミット名義は公開用設定を明示する。接続サービスが個人メールを強制する場合はpushを止める。履歴の後消しを公開手順にしない。
- テスト未実施を合格と書かない。人のコードレビューを品質保証の前提にしない。

## 完了

実装・仕様・回帰テスト・運用手順が一致し、品質ゲートが成功した変更だけを完了とする。
実サーバーへのデプロイ、権限変更、実データの移行は別途明示的な承認が必要。
